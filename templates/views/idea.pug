extends ../layouts/default

block content
	.container: .row: .col-sm-10.col-sm-offset-1.col-md-8.col-md-offset-2
		article
			p: a(href='/ideas') &larr; back to all ideas
			hr
			if !data.idea
				h2 Invalid Post.
			else
				header
					h1= data.idea.title
					h5 Posted 
						if data.idea.publishedDate
							| on #{data.idea._.publishedDate.format('MMMM Do, YYYY')} 
						if data.idea.categories && data.idea.categories.length
							| in 
							each cat, cat_i in data.idea.categories
								a(href='/idea/' + cat.key)= cat.name
								if cat_i < data.idea.categories.length - 1
									| , 
								else
									|  
						if data.idea.author
							| by #{data.idea.author.name.first}
				.idea
					if data.idea.image.exists
						.image-wrap: img(src=data.idea._.image.fit(750,450)).img-responsive
					!= data.idea.content.full
